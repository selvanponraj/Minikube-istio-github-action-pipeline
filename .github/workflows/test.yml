name: Test Runner
on:
  push:
    branches:
      - update-doc
jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Print Debug Info
        run: |
          echo "Hello Team from $(whoami) on $(hostname)"
          date
          echo "PATH: $PATH"
